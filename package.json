
{
  "name": "static",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "chmod +x ./node_modules/.bin/tsc && tsc"
  }
}